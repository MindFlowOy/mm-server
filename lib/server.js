// Generated by CoffeeScript 1.6.3
(function() {
  "use strict";
  /*
  * ---
  *   Server module
  *   @name server
  *   @api public
  */

  var Hapi, exports;

  Hapi = require('hapi');

  exports = module.exports = function(config) {
    var plugins, retVal, serverInst;
    retVal = {
      server: void 0,
      error: void 0
    };
    if (config.host && config.port && config.options && config.plugins) {
      serverInst = new Hapi.Server(config.host, config.port, config.options);
      plugins = config.plugins;
      if (plugins.travelogue) {
        plugins.travelogue = config;
      }
      serverInst.pack.allow({
        ext: true
      }).require(plugins, function(err) {
        if (err) {
          return retVal.error = err || "Server module: Unexpected error! (hapi.plugins)";
        } else {
          return retVal.server = serverInst;
        }
      });
    } else {
      retVal.error = "Server module: Invalid configuration object";
    }
    return retVal;
  };

}).call(this);
